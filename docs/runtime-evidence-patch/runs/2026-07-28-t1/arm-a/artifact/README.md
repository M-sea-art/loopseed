# 雨夜客栈：守灯人

一个零依赖、单屏、三夜制的浏览器叙事游戏垂直切片。

## 运行

在本目录执行：

```bash
python3 -m http.server 4173 --bind 127.0.0.1
```

然后在浏览器访问 `http://127.0.0.1:4173/`。

## 玩法

每夜只能选择一名旅客，并为其目的消耗对应灯火。选择后会立即出现后果；第三夜结束后，三次选择的组合会生成不同结局。结局页可重新开始。

声音按钮会使用 Web Audio 在本地合成雨声与提示音，不读取任何外部资源。
