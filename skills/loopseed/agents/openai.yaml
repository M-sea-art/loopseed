interface:
  display_name: "LoopSeed"
  short_description: "Run minimal evidence loops or One-Shotted completion"
  default_prompt: "$loopseed one-shotted Complete this goal from one authorization, with independent evidence before finalization."
policy:
  allow_implicit_invocation: false
