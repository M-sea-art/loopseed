interface:
  display_name: "LoopSeed"
  short_description: "Start a plan-bound evidence loop from a minimal goal"
  default_prompt: "$loopseed Follow the current project plan and complete the active milestone with direct evidence."
policy:
  allow_implicit_invocation: false
