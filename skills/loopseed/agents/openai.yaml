interface:
  display_name: "LoopSeed"
  short_description: "Aim game ideas, then launch verified One-Shot production"
  default_prompt: "$loopseed one-shotted Co-direct and lock this game idea, then run no-idle controlled Fan-out until independently verified or truly blocked."
policy:
  allow_implicit_invocation: false
